#include <stdio.h>
/**
* main-A prgram that prints all single digit numbers of base 10 starting from 0
* Return: 0 (Success)
 */
int main(void)
{
	int i = 48;

	for (i = 48; i <= 57; i++)
	{
		putchar((int)i);
	}
	ptchar('\n');
	return (0);
}
