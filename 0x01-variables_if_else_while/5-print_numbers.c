#include <stdio.h>
/**
* main-A prgram that prints all single digit numbers of base 10 starting from 0
* Return: 0 (Success)
*/
int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf(i);
	}
	printf('\n');
	return (0);
}
