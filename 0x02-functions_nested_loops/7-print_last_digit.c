#include"main.h"
/**
 * print_last_digit - a function that prints the last digit of a number
 * @n : the number that its last digit will be printed
 * Return: the value of the last digit
 */
int print_last_digit(int n)
{
	int last_digit = n % 10;

	return (last_digit);
}
